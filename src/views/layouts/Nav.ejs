<!-- Navbar -->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <!-- Container wrapper -->
  <div class="container">
    <!-- Navbar brand -->
    <a class="navbar-brand me-2" href="/">
      <i class="fa-solid fa-bag-shopping" style="color: #5897fb"></i>
    </a>

    <!-- Toggle button -->
    <button
      class="navbar-toggler"
      type="button"
      data-mdb-toggle="collapse"
      data-mdb-target="#navbarButtonsExample"
      aria-controls="navbarButtonsExample"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <i class="fas fa-bars"></i>
    </button>

    <!-- Collapsible wrapper -->
    <div class="collapse navbar-collapse" id="navbarButtonsExample">
      <!-- Left links -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="/">Online Shopping</a>
        </li>
        <li class="nav-item">
          <a class="nav-link fw-bold" href="/">Home</a>
        </li>
        <% if(owner) { %>
        <li class="nav-item">
          <a class="nav-link fw-bold" href="/api/products/create"
            >Create Product</a
          >
        </li>
        <% } %>
      </ul>

      <!-- Left links -->
      <% if(owner) { %>
      <div class="d-flex align-items-center">
        <a href="/api/owner/profile/<%= owner._id %>">
          <button
            type="button"
            class="btn btn-link px-3 me-2"
            data-mdb-toggle="tooltip"
            data-mdb-html="true"
            title="<em>Hi</em> <u><%= owner.name %></u>"
          >
            <%= owner.name %>
          </button>
        </a>
        <form action="/api/owners/auth/logout">
          <button
            type="submit"
            class="btn me-3 text-white"
            style="background-color: #5897fb"
          >
            logOut
          </button>
        </form>
      </div>
      <% } else { %>
      <!-- Left links -->
      <div class="d-flex align-items-center">
        <a href="/api/owners/auth/login"
          ><button type="button" class="btn btn-link px-3 me-2">
            Login
          </button></a
        >
        <a href="/api/owners/auth/create">
          <button
            type="button"
            class="btn me-3 text-white"
            style="background-color: #5897fb"
          >
            Sign up for free
          </button></a
        >
      </div>
      <% } %>
    </div>
    <!-- Collapsible wrapper -->
  </div>
  <!-- Container wrapper -->
</nav>
<!-- Navbar -->
